import 'package:flutter/material.dart';

class AirpalyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Airpaly'),),
      body: Center(child: Text('Airpaly'),),
    );
  }
}